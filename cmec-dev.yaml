spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/cmecdb
    username: cmec
    password: cmec
  jpa:
    properties:
      hibernate:
       dialect: org.hibernate.PostgreSQL.Dialect
       show_sql: true


server:
  port: ${SERVER_PORT:8010}


